<%= form_with(model: starred_post) do |form| %>
  <% if starred_post.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(starred_post.errors.count, "error") %> prohibited this starred_post from being saved:</h2>

      <ul>
        <% starred_post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
