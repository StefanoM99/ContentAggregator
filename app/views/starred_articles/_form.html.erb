<%= form_with(model: starred_article) do |form| %>
  <% if starred_article.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(starred_article.errors.count, "error") %> prohibited this starred_article from being saved:</h2>

      <ul>
        <% starred_article.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
