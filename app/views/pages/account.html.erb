<div id="helpDiv">
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li id="backBtn" class="breadcrumb-item" style="cursor: pointer;"><p>Help</p></li>
            <li class="breadcrumb-item active" aria-current="page">Account</li>
        </ol>
    </nav>
    <p class="line-1 anim-typewriter font-monospace pt-6">Here is what I can do :)</p>
    
    <div class="accordion accordion-flush" id="accountAccordionFlush">
        <div class="accordion-item">
            <h2 class="accordion-header" id="flush-headingOne">
                <button class="accordion-button collapsed font-monospace" type="button" data-bs-toggle="collapse" data-bs-target="#flush-collapseOne" aria-expanded="false" aria-controls="flush-collapseOne">
                > Update my email
                </button>
            </h2>
            <div id="flush-collapseOne" class="accordion-collapse collapse" aria-labelledby="flush-headingOne" data-bs-parent="#accountAccordionFlush">
                <div class="accordion-body">
                    <%= form_with(model: current_user) do |form| %>
                        <div class="mb-3">
                            <label for="inputEmail" class="form-label">Enter the new email:</label>
                            <%= form.text_field :email, class: "form-control", :disabled => (current_user.provider!=nil) %>
                        </div>
                        <%= form.submit "Submit", class: "btn btn-primary", :disabled => (current_user.provider!=nil) %>
                    <% end %>
                </div>
            </div>
        </div>
        <div class="accordion-item">
            <h2 class="accordion-header" id="flush-headingTwo">
                <button class="accordion-button collapsed font-monospace" type="button" data-bs-toggle="collapse" data-bs-target="#flush-collapseTwo" aria-expanded="false" aria-controls="flush-collapseTwo">
                > Update my password
                </button>
            </h2>
            <div id="flush-collapseTwo" class="accordion-collapse collapse" aria-labelledby="flush-headingTwo" data-bs-parent="#accountAccordionFlush">
                <div class="accordion-body">
                    <%= form_with(model: current_user) do |form| %>
                        <div class="field mb-3">
                            <%= form.label :password, "Enter new password" %><br />
                            <%= form.password_field :password, :autocomplete => "off", class: "form-control", :disabled => (current_user.provider!=nil) %>
                        </div>
                        <div class="field mb-3">
                            <%= form.label :password_confirmation, "New password confirmation" %><br />
                            <%= form.password_field :password_confirmation, class: "form-control", :disabled => (current_user.provider!=nil) %>
                        </div>
                        <div class="action_container">
                            <%= form.submit "Submit", class: "btn btn-primary", :disabled => (current_user.provider!=nil) %>
                        </div>
                    <% end %>
                </div>
            </div>
        </div>
        <div class="accordion-item">
            <h2 class="accordion-header" id="flush-headingThree">
                <button class="accordion-button collapsed font-monospace" type="button" data-bs-toggle="collapse" data-bs-target="#flush-collapseThree" aria-expanded="false" aria-controls="flush-collapseThree">
                > Delete my account
                </button>
            </h2>
            <div id="flush-collapseThree" class="accordion-collapse collapse" aria-labelledby="flush-headingThree" data-bs-parent="#accountAccordionFlush">
                <div class="accordion-body">
                    <div class="text-danger font-monospace">
                    <u>WARNING</u>: you are about to delete your account. Your name, public profile and username will no longer be available on the site.
                    </div>
                    <div class="d-grid gap-2 col-5 mx-auto my-3">
                        <%= button_to "Delete account", current_user, method: :delete, class: "btn btn-danger fw-semibold p-3" %>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>