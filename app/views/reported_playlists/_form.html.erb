<%= form_with(model: reported_playlist) do |form| %>
  <% if reported_playlist.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(reported_playlist.errors.count, "error") %> prohibited this reported_playlist from being saved:</h2>

      <ul>
        <% reported_playlist.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
