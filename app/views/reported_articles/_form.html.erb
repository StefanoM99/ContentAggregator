<%= form_with(model: reported_article) do |form| %>
  <% if reported_article.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(reported_article.errors.count, "error") %> prohibited this reported_article from being saved:</h2>

      <ul>
        <% reported_article.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
